@model IEnumerable<AdmissionsWebsite.AdmissionDecision>

@{
    ViewBag.Title = "Index";
}

@if (Request.IsAuthenticated && User.IsInRole("Admin"))
{
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DecisionType)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DecisionType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DecisionId }) |
                @Html.ActionLink("Details", "Details", new { id = item.DecisionId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DecisionId })
            </td>
        </tr>
    }

    </table>
}
